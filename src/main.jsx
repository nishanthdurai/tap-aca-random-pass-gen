/* eslint-disable no-undef */
// library
import { Typography, Box } from '@mui/material';

const Main = () => {
  return (
    <Box>
      <Typography>Hello world</Typography>
    </Box>
  );
};

export default Main;
